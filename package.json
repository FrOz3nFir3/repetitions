{
  "name": "repetitions.tech",
  "version": "1.0.0",
  "description": "full stack project",
  "main": "index.js",
  "engines": {
    "node": "16.13.2",
    "npm": "8.1.2"
  },
  "scripts": {
    "server": "npm run watch --prefix server",
    "client": "npm run watch --prefix client",
    "dev": "npm-run-all --parallel server client",
    "install": "npm run install --prefix client && npm run install --prefix server",
    "build": "npm run build --prefix client && npm run start --prefix server"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "npm-run-all": "^4.1.5"
  }
}
